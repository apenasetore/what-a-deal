import Config

# Categorias de interesse do cliente consumidor (hardcoded por simplicidade,
# conforme permitido pelo enunciado). Pode ser sobrescrito por instancia.
config :cliente,
  categorias: ["livro", "jogo"],
  destaque: true
